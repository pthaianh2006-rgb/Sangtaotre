# Tested together on Python 3.12.10. Check with: python -m pip check
# Web, measurement export and serial device support
Flask==3.1.3
Werkzeug==3.1.8
openpyxl==3.1.5
numpy==2.5.0
# opencv-contrib-python (superset of opencv-python) is what mediapipe requires;
# avoid also installing opencv-python or the two cv2 builds conflict.
opencv-contrib-python==4.13.0.92
# The camera pipeline uses the legacy mp.solutions.pose API. Do not upgrade
# MediaPipe independently without migrating that pipeline and validating angles.
mediapipe==0.10.14
pyserial==3.5

# Tkinter live plot and Word report generation (tien_ich/gen_report.py)
matplotlib==3.11.0
python-docx==1.2.0
